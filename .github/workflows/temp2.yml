name: sample file
on:
  workflow_dispatch

jobs: 
  job1:
    strategy:
      matrix: 
        os: [windows-latest, ubuntu-latest]
    
    runs-on: ${{ matrix.os }}
    steps:
      - name: display
        run: |
          echo "runner name: ${{matrix.os}}"


      
